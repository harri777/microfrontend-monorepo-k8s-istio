const messageExample = () => {
	return "it's my message example with monorepo at microfrontends";
};

const titleApplication = () => {
	return 'My title shared application.'
};

module.exports = { messageExample, titleApplication };